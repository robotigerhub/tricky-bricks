local c = require("modules.common")

local move_up

function on_message(self, message_id, message, sender)
	if message_id == hash("move_up") then
		move_up()
	end
end

function move_up()
	go.set_position(go.get_position() + vmath.vector3(0, c.SPAWNPOINT_RAISE, 0))
end